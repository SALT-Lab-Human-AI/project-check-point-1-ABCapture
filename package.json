{
  "name": "abcapture-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd concept-iteration && npm install && npm run build",
    "start": "cd concept-iteration && npm run start",
    "dev": "cd concept-iteration && npm run dev"
  }
}
